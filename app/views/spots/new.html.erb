<% content_for(:title, 'スポット登録') %>
<% content_for(:body_class, "spot") %>
<% content_for(:body_id, "index") %>

<section class="area container">
  <h1>スポット登録</h1>
  
  <%= render 'form' %>
</section>

